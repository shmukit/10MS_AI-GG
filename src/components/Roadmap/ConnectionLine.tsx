import React from 'react';

// This component is no longer needed for the path-style layout
// but keeping it for potential future use
export const ConnectionLine: React.FC = () => {
  return null;
};